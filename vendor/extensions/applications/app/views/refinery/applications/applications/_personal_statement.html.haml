= f.inputs 'Personal statement' do
  = f.input :statement, :required => true, :hint => "Please don't feel limited by the size of the box! We want to hear what you have to say.", :input_html => {:disabled => disabled}
